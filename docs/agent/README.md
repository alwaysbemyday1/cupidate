# Agent Docs

This folder is the single source of truth for agent workflow and execution records.

Start here:
- `codexrules.md`: domain/design/workflow baseline rules
- `feature_list.json`: product scope, view map, service phases
- `progress.txt`: current execution status and next action queue

Primary references:
- `design_system.md`: visual direction + tokens + components + assets + rebuild roadmap
- `domain_data.md`: matching input schema + scoring policy + repository contracts
- `worklog.md`: chronological implementation log (recent focus)

View specs (kept separate by request):
- `views/home.md`
- `views/network.md`
- `views/matching.md`
- `views/my.md`

Reference assets:
- `../design/*.png`
